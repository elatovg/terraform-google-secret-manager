---
provisioner:
  name: terraform

platforms:
- name: local

transport:
  name: exec
  connection_timeout: 60

verifier:
  name: terraform
  systems:
    - name: system
      backend: local

suites:
  - name: secret-manager
    driver:
      name: terraform
      command_timeout: 1800
      verify_version: false
      root_module_directory: test/fixtures/secret-manager